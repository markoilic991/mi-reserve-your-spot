1. officeSpace service findAll investigate why fetch type lazy do not work //low priority
+ 2. method cancelReservation - try delete query directly instead of select + delete
+ 3. officeSpace service delete - check if this method delete officeRooms and working stations that belong to this OfficeSpace + if reservations are deleted (for these working stations)
4. officeSpace service save - check how method save works when we need to update existing entity (when providing only id and description, write test)
+ 5. officeRoom controller - create method findByOfficeSpaceId
+ 6. rewrite application.properties to have same values as docker compose file
+ 7. add mainClassName and plugin Id application inside buildGradle
----------------------

1. clean up slash from controllers route (localhost:8080/users/)
2. OfficeRoom - check if we can create, update or delete officeRoom
3. WorkStation controller - create method findByOfficeRoomId
4. WorkStation - check if we can create, update or delete workStation
5. officeSpace add name column + unique constrait
6. user unique constrait for email
7. change orderNo to code and set field to be unique inside officeRoom
8. change uniqueCode to code and set field to be unique inside WorkStation
9. make sure that SampleDataService create data only once
10. update log4j dependency - 2.17.0
----------------------

1. create docker image
2. how to start up a container using docker image
3. fix README file
4. push this image to docker hub
5. research wy joinColumn annotation is not applied (@JoinColumn(name = "officeSpace_Id"))
6. remove unnecessary JOIN to fetch parent object from queries
----------------------

1. User reservation view - analysis and preparation

